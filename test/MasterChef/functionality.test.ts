/**
 * Functionality Test for MasterChef
 * It should work for each function, and revert with reason correctly.
 */

describe("MasterChef Reward Functionality", () => {
  before(() => {
    // Inits a VotingPower Mock instance, grants signer some VP
    // Inits a MasterChef and some Pools instance, grants signer some LP,
    //  set the reward for each time window
  });
});
